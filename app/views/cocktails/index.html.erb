<h1>Cocktails</h1>

<%= link_to 'New cocktail', new_cocktail_path(@cocktails), :class => "btn btn-success" %>
<% @cocktails.each do |cocktail| %>
<h4><%= link_to cocktail.name, cocktail_path(cocktail) %></h4>
<% end %>
